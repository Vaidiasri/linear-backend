# Docker Environment Configuration

# ⚠️ SECURITY WARNING ⚠️
# Copy this file to .env and update with your values
# NEVER commit .env file to git
# ALL values below are REQUIRED - no defaults provided

# PostgreSQL Configuration (for Docker)
# Use strong passwords in production!
POSTGRES_USER=linearuser
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
POSTGRES_DB=lineardb

# Database URL (Docker service name 'postgres' is used as host)
DATABASE_URL=postgresql+asyncpg://linearuser:CHANGE_THIS_TO_STRONG_PASSWORD@postgres:5432/lineardb

# JWT Configuration
# Generate SECRET_KEY with: openssl rand -hex 32
SECRET_KEY=CHANGE_THIS_GENERATE_WITH_OPENSSL_RAND_HEX_32
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Production Security Checklist:
# 1. ✅ Change POSTGRES_PASSWORD to a strong password (min 16 chars)
# 2. ✅ Generate SECRET_KEY: openssl rand -hex 32
# 3. ✅ Update DATABASE_URL with your POSTGRES_PASSWORD
# 4. ✅ Keep .env file secure and never commit to git
# 5. ✅ Use different credentials for production vs development
