# Database Configuration
# For local development (without Docker)
# DATABASE_URL=postgresql+asyncpg://username:password@localhost/database_name

# For Docker deployment (use these settings)
DATABASE_URL=postgresql+asyncpg://linearuser:linearpass@postgres:5432/lineardb

# PostgreSQL Docker Configuration
POSTGRES_USER=linearuser
POSTGRES_PASSWORD=linearpass
POSTGRES_DB=lineardb

# JWT Configuration
SECRET_KEY=your-super-secret-key-change-in-production-minimum-32-characters
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

